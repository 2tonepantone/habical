<nav class="navbar navbar-light bg-light">
  <div class="container-fluid">
    <div class="navbar-brand">
      <%= link_to "", :root, class:'brand-link' %>
      <i class="far fa-calendar-check"></i>
      HabiCal
    </div>
    <% if user_signed_in? %>
      <div class="nav-item dropdown">
        <a class="nav-link px-1" href="#" id="navbarDropdown" role="button"
        data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          <i class="fas fa-bars"></i>
        </a>
        <div class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
          <% username = current_user.email.split('@')[0].capitalize %>
          <div class="dropdown-item disabled">
            <span>Welcome, <%= username %>!</span>
          </div>
          <li><hr class="dropdown-divider"></li>
          <%= link_to "Profile", edit_user_registration_path , class: "dropdown-item" %>
          <li><hr class="dropdown-divider"></li>
          <%= link_to "Log out", destroy_user_session_path, method: :delete, class: "dropdown-item" %>
        </div>
      </div>
    <% else %>
      <%= link_to "Login", user_google_oauth2_omniauth_authorize_path, class: "btn btn-outline-primary btn-sm" %>
    <% end %>
  </div>
</nav>
