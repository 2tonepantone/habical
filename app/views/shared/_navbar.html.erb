<nav class="navbar navbar-light bg-light">
  <div class="container-fluid">
    <div class="navbar-brand">
      <%= link_to "", :root, class:'brand-link' %>
      <i class="far fa-calendar-check"></i>
      HabiCal
    </div>
    <% if current_user %>
      <div class="nav-item dropdown">
        <a class="nav-link px-1" href="#" id="navbarDropdown" role="button"
        data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          <i class="fas fa-bars"></i>
        </a>
        <div class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
          <div class="dropdown-item disabled">
            <span>Welcome, <%= current_user.name %>!</span>
          </div>
          <div class="dropdown-item">
            <%= link_to "Logout", destroy_user_session_path, :method => :delete %>
          </div>
        </div>
      </div>
    <% else %>
      <%= button_to "Login", user_google_oauth2_omniauth_authorize_path,
            method: :post, class: "btn btn-outline-primary", form: {"data-turbo" => "false"} %>
    <% end %>
  </div>
</nav>
